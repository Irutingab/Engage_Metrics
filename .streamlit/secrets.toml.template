# This is a template for secrets (don't commit the actual secrets.toml file!)
# Copy this to .streamlit/secrets.toml and fill in your actual values

# OpenAI API Key (if using cloud AI)
# OPENAI_API_KEY = "sk-your-actual-api-key-here"

# Database connection (if using PostgreSQL)
# DATABASE_URL = "postgresql://user:password@host:port/database"

# Other secrets
# SECRET_KEY = "your-secret-key-here"

# Example for Streamlit Cloud:
# 1. Go to your app settings on share.streamlit.io
# 2. Click "Secrets" in the left sidebar
# 3. Paste the secrets there (without this header comment)
